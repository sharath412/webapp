spring:
  application:
    name: webapp
  cloud:
    consul:
      host: 172.19.0.2
      port: 8500
      config:
        enabled: false
      discovery:
        enabled: true
        instance-id: ${spring.cloud.client.hostname}:${spring.application.name}:${random.int[1,999999]}"